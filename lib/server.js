/*▄──────────▄
  █          █
  █  Сервер  █
  █          █
  ▀──────────▀*/
let _collection = null; // Коллекция обработчиков

/*┌────────────────────────────────────────┐
  │ Добавляет новую коллекцию обработчиков │
  └────────────────────────────────────────┘*/
Object.defineProperty($.wifi, 'server', {set: (item) => {
    _collection = new item;
    _=_collection
}});
